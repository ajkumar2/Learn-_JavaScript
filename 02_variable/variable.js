const name = "Ajay Kumar"  // define const mean not changeable
let roll_no = 12
var student_class = "fivth"
area = 45  // not used in development purpose ,  define datatype like var, let, const
let total_area;

console.log(name);
console.table([name, roll_no, student_class, area, total_area])
